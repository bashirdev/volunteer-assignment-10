import React, { useContext } from 'react';
import { UserContext } from '../../App';

const Blog = () => {

    return (
        <div>
            <h1>this blog page</h1>
        </div>
    );
};

export default Blog;